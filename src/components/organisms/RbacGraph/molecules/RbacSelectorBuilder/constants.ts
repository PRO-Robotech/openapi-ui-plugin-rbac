import { TFilterKey } from './types'

export const FILTER_LABELS: Record<TFilterKey, string> = {
  verbs: 'Verbs',
  nonResourceURLs: 'Non-Resource URLs',
}

export const CORE_GROUP_VALUE = '__core__'
